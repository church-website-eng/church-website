# ===========================================
# CCC Goshen Cathedral — Environment Variables
# ===========================================

# Database (Neon PostgreSQL — free at https://neon.tech)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# NextAuth
NEXTAUTH_SECRET="generate-a-random-secret-here"
NEXTAUTH_URL="https://goshencathedral.ca"

# Vercel Blob (auto-configured when you add Blob storage in Vercel dashboard)
BLOB_READ_WRITE_TOKEN=""

# SMTP (for email notifications — e.g., Gmail, SendGrid, Resend)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
ADMIN_EMAIL=""

# Stripe (optional — if re-enabling online payments)
# STRIPE_SECRET_KEY=""
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Contentful CMS (optional — for sermons/events/blog from CMS)
# CONTENTFUL_SPACE_ID=""
# CONTENTFUL_ACCESS_TOKEN=""
# CONTENTFUL_PREVIEW_TOKEN=""

# Google OAuth (optional — for member login via Google)
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""
